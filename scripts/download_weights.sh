#!/usr/bin/env bash
# download_weights.sh — 모델 가중치 자동 다운로드 스크립트.
# *.pt/*.pth/*.onnx는 .gitignore 대상이므로 외부 스토리지에서 받아온다.

set -euo pipefail

# TODO: 가중치 URL/체크섬 정의 및 다운로드 로직 구현
# WEIGHTS_URL="..."
# OUTPUT_DIR="backend/models/weights"
# mkdir -p "${OUTPUT_DIR}"
# curl -L "${WEIGHTS_URL}" -o "${OUTPUT_DIR}/baseline.pt"

echo "TODO: implement weight download"
