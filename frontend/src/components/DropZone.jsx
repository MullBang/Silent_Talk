// DropZone.jsx — 드래그 앤 드롭 / 파일 선택 업로드 영역.

import React from 'react';

/**
 * 영상 파일 드롭존 컴포넌트.
 * @param {{ onFileSelected: (file: File) => void }} props
 * @returns {JSX.Element}
 */
export default function DropZone({ onFileSelected }) {
  // TODO: 드래그 이벤트 처리, 파일 선택 핸들링
  return null;
}
