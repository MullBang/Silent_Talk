// ProgressBar.jsx — 추론 진행률 표시 바.

import React from 'react';

/**
 * 진행률 바 컴포넌트.
 * @param {{ progress: number }} props - 0~1 정규화 진행률.
 * @returns {JSX.Element}
 */
export default function ProgressBar({ progress }) {
  // TODO: 진행률 시각화
  return null;
}
