// ResultList.jsx — 추론 결과 목록 렌더링.

import React from 'react';

/**
 * 결과 리스트 컴포넌트.
 * 각 항목의 transcript, confidence(0~1), raw_score를 표시한다.
 * @param {{ results: Array<{ transcript: string, confidence: number, raw_score: number }> }} props
 * @returns {JSX.Element}
 */
export default function ResultList({ results }) {
  // TODO: 결과 항목 매핑 렌더링
  return null;
}
